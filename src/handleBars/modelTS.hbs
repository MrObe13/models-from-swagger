{{#each import}}
import {{this}} from '../{{this}}';
{{/each}}

export default class Base{{getKey}} {
  protected static attrTypes: Attributes = {
 {{#each properties}}
    {{@key}}: {{{stringfyObj this}}},
 {{/each}}
  };
}
