import Base{{getKey}} from './baseModel/Base{{getKey}}';

export default class {{getKey}} extends Base{{getKey}} {

  // static idAttribute = 'id';
  // static subClasses = {};
  // static discriminator = null;
}
